import{fireEvent,render,screen,waitFor}from"@testing-library/react";import{beforeEach,describe,expect,it,vi}from"vitest";import MemoryJournal from"./MemoryJournal";import{api}from"../api/client";
vi.mock("../api/client",()=>({getCurrentUserId:()=>1,api:{listMemoryJournal:vi.fn(),loadMemoryJournalPhoto:vi.fn(),updateMemoryJournal:vi.fn(),deleteMemoryJournal:vi.fn()}}));
const child={id:1,name:"Nara"};const response={can_create:true,can_manage_storage:false,items:[{id:4,caption:"Main di taman",occurred_date:"2026-08-20",tags:["keluarga"],is_favorite:true,can_edit:true,can_delete:true}]};
describe("MemoryJournal search metadata",()=>{beforeEach(()=>{vi.clearAllMocks();api.listMemoryJournal.mockResolvedValue(response);api.loadMemoryJournalPhoto.mockRejectedValue(new Error());});it("renders favorite and tags",async()=>{render(<MemoryJournal child={child}/>);expect(await screen.findByText(/Main di taman/)).toBeInTheDocument();expect(screen.getByLabelText("Favorit",{selector:"span"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"#keluarga"})).toBeInTheDocument();});it("submits caption search and favorite filter",async()=>{render(<MemoryJournal child={child}/>);await screen.findByText(/Main di taman/);fireEvent.change(screen.getByPlaceholderText("Cari caption atau tag"),{target:{value:"taman"}});fireEvent.click(screen.getByLabelText("Favorit",{selector:"input"}));fireEvent.click(screen.getByRole("button",{name:"Cari"}));await waitFor(()=>expect(api.listMemoryJournal).toHaveBeenLastCalledWith(1,{q:"taman",favorite:"true",sort:"newest"}));});it("toggles favorite using metadata update",async()=>{api.updateMemoryJournal.mockResolvedValue({});render(<MemoryJournal child={child}/>);fireEvent.click(await screen.findByRole("button",{name:"Hapus dari favorit"}));await waitFor(()=>expect(api.updateMemoryJournal).toHaveBeenCalledWith(4,{is_favorite:false}));});});
