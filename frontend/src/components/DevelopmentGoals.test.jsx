import{fireEvent,render,screen,waitFor}from"@testing-library/react";import{beforeEach,describe,expect,it,vi}from"vitest";import DevelopmentGoals from"./DevelopmentGoals";import{api}from"../api/client";
vi.mock("../api/client",()=>({api:{listDevelopmentGoals:vi.fn(),createDevelopmentGoal:vi.fn(),completeDevelopmentGoal:vi.fn(),reopenDevelopmentGoal:vi.fn(),updateDevelopmentGoal:vi.fn(),deleteDevelopmentGoal:vi.fn()}}));
const child={id:1,name:"Nara"};const response={can_create:true,disclaimer:"Bukan penilaian medis.",items:[{id:3,category:"growth_check",title:"Ukur tinggi",target_date:"2026-08-26",note:null,state:"due",can_complete:true,can_reopen:false,can_edit:true}]};
describe("DevelopmentGoals",()=>{beforeEach(()=>{vi.clearAllMocks();api.listDevelopmentGoals.mockResolvedValue(response);});it("shows readable goal state and disclaimer",async()=>{render(<DevelopmentGoals child={child} onClose={vi.fn()}/>);expect(await screen.findByText("Ukur tinggi")).toBeInTheDocument();expect(screen.getByText("Minggu ini")).toBeInTheDocument();expect(screen.getByText("Bukan penilaian medis.")).toBeInTheDocument();});it("completes a goal",async()=>{api.completeDevelopmentGoal.mockResolvedValue({});render(<DevelopmentGoals child={child} onClose={vi.fn()}/>);fireEvent.click(await screen.findByRole("button",{name:"Tandai selesai"}));await waitFor(()=>expect(api.completeDevelopmentGoal).toHaveBeenCalledWith(3));});it("creates a safe family goal",async()=>{api.createDevelopmentGoal.mockResolvedValue({});render(<DevelopmentGoals child={child} onClose={vi.fn()}/>);fireEvent.click(await screen.findByRole("button",{name:"+ Tujuan"}));fireEvent.change(screen.getByPlaceholderText("Contoh: Jadwalkan ukur tinggi"),{target:{value:"Latihan tengkurap"}});fireEvent.click(screen.getByRole("button",{name:"Simpan"}));await waitFor(()=>expect(api.createDevelopmentGoal).toHaveBeenCalled());});});
