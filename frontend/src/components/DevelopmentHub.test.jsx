import{fireEvent,render,screen}from"@testing-library/react";import{describe,expect,it,vi}from"vitest";import DevelopmentHub from"./DevelopmentHub";
vi.mock("./DevelopmentCalendar",()=>({default:({onClose})=><div>Calendar view<button onClick={onClose}>Back calendar</button></div>}));vi.mock("./DevelopmentGoals",()=>({default:()=> <div>Goals view</div>}));vi.mock("./FamilyDevelopmentCheckIn",()=>({default:()=> <div>Check-in view</div>}));vi.mock("./MonthlyStory",()=>({default:()=> <div>Story view</div>}));
const child={id:2,name:"Nara"};
describe("DevelopmentHub",()=>{it("groups development tools into readable cards",()=>{render(<DevelopmentHub child={child} onClose={vi.fn()}/>);expect(screen.getByText("Development Hub")).toBeInTheDocument();expect(screen.getByRole("button",{name:/Kalender/})).toBeInTheDocument();expect(screen.getByRole("button",{name:/Check-in/})).toBeInTheDocument();expect(screen.getByRole("button",{name:/Tujuan/})).toBeInTheDocument();expect(screen.getByRole("button",{name:/Cerita Bulanan/})).toBeInTheDocument();});it("opens a feature and returns to the hub",()=>{render(<DevelopmentHub child={child} onClose={vi.fn()}/>);fireEvent.click(screen.getByRole("button",{name:/Kalender/}));expect(screen.getByText("Calendar view")).toBeInTheDocument();fireEvent.click(screen.getByText("Back calendar"));expect(screen.getByText("Development Hub")).toBeInTheDocument();});it("closes the hub",()=>{const close=vi.fn();render(<DevelopmentHub child={child} onClose={close}/>);fireEvent.click(screen.getByRole("button",{name:"Tutup"}));expect(close).toHaveBeenCalled();});});
