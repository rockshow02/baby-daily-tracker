import{fireEvent,render,screen,waitFor}from"@testing-library/react";import{beforeEach,describe,expect,it,vi}from"vitest";import FamilyDevelopmentCheckIn from"./FamilyDevelopmentCheckIn";import{api}from"../api/client";
vi.mock("../api/client",()=>({api:{listFamilyCheckIns:vi.fn(),createFamilyCheckIn:vi.fn(),updateFamilyCheckIn:vi.fn(),deleteFamilyCheckIn:vi.fn()}}));
const child={id:4,name:"Nara"};const response={items:[{id:2,period_month:"2026-08",areas:{motor:"noticed",communication:"exploring",social:"not_checked",sleep:"not_checked",nutrition:"noticed"},reflection_note:"Suka bermain",discuss_with_professional:true,linked_goal_id:null,linked_goal_title:null,created_by_name:"Bunda",can_edit:true,can_delete:true}],goals:[],can_create:true,disclaimer:"Check-in ini adalah refleksi keluarga, bukan skrining, diagnosis, atau penilaian."};
describe("FamilyDevelopmentCheckIn",()=>{beforeEach(()=>{vi.clearAllMocks();api.listFamilyCheckIns.mockResolvedValue(response);});it("renders a non-diagnostic monthly reflection",async()=>{render(<FamilyDevelopmentCheckIn child={child} onClose={vi.fn()}/>);expect(await screen.findByText("Suka bermain")).toBeInTheDocument();expect(screen.getByText(/bukan skrining/)).toBeInTheDocument();expect(screen.getByText("Bahas saat konsultasi")).toBeInTheDocument();});it("creates a check-in",async()=>{api.createFamilyCheckIn.mockResolvedValue({});render(<FamilyDevelopmentCheckIn child={child} onClose={vi.fn()}/>);fireEvent.click(await screen.findByRole("button",{name:"+ Check-in"}));fireEvent.change(screen.getByLabelText("Gerak & motorik"),{target:{value:"noticed"}});fireEvent.click(screen.getByRole("button",{name:"Simpan"}));await waitFor(()=>expect(api.createFamilyCheckIn).toHaveBeenCalledWith(4,expect.objectContaining({areas:expect.objectContaining({motor:"noticed"})})));});it("opens an existing reflection for editing",async()=>{api.updateFamilyCheckIn.mockResolvedValue({});render(<FamilyDevelopmentCheckIn child={child} onClose={vi.fn()}/>);fireEvent.click(await screen.findByRole("button",{name:"Edit"}));fireEvent.change(screen.getByPlaceholderText("Hal yang ingin diingat keluarga"),{target:{value:"Diperbarui"}});fireEvent.click(screen.getByRole("button",{name:"Simpan"}));await waitFor(()=>expect(api.updateFamilyCheckIn).toHaveBeenCalledWith(2,expect.objectContaining({reflection_note:"Diperbarui"})));});});
