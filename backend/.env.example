SECRET_KEY=ganti-dengan-random-string-panjang
FLASK_ENV=development
FRONTEND_ORIGIN=http://localhost:5173

# Opsional — ditampilkan di field "version" GET /api/health (lihat
# backend/docs/OBSERVABILITY.md). Disaring lewat whitelist regex
# ^[A-Za-z0-9._-]{1,40}$ sebelum dipakai; kalau nggak diisi/nggak valid,
# fallback ke "unknown". APP_VERSION diprioritaskan di atas DEPLOY_COMMIT.
# APP_VERSION=1.4.0
# DEPLOY_COMMIT=abc1234
